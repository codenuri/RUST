fn main()
{
	let score = 80;

//	let total = if score > 60 { 5 } else { 0 };

//	let total = if score > 60 { 5; } else { 0; };

//	let total = if score > 60 { 5; } else { 0 };

	let total = if score > 60 { 5 };

	println!("{:?}", total);
}
